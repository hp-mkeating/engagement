defaults {
       user_friendly_names             no
       polling_interval                10
       max_fds                         8192
}

devices {
       device {
               vendor                  "3PARdata"
               product                 "VV"
               features                "0"
               hardware_handler        "1 alua"
               path_selector           "round-robin 0"
               getuid_callout          "/lib/udev/scsi_id --whitelisted --device=/dev/%n"
               path_grouping_policy    "group_by_prio"
               prio                    "alua"
               failback                "immediate"
               rr_weight                "uniform"
               no_path_retry            18
               rr_min_io_rq             1
               path_checker             "tur"
        }
}


# yum install device-mapper-multipath sg3_utils device-mapper
# service multipathd restart
# chkconfig multipathd on
# /usr/bin/rescan-scsi-bus.sh
# multipath
# multipath -ll
